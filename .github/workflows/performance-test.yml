name: 'DISABLED - Performance Testing'

# This workflow is disabled because it's too complex and blocks merges
# with environment variable issues and Redis connection problems

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why is this disabled?'
        required: false
        default: 'Too complex, blocks merges'

jobs:
  disabled:
    name: 'DISABLED - Performance Testing'
    runs-on: ubuntu-latest
    if: false  # Never runs
    steps:
      - name: 'This workflow is disabled'
        run: |
          echo "Performance testing workflow is disabled"
          echo "Reason: Too complex and blocks merges"
          echo "Environment variable issues and Redis connection problems"
